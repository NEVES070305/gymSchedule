@model Rede
@{
    ViewData["Title"] = "Cadastrar rede";
}
<div class="text-center">
    <div class="container">
        <div class="row">
            <div class="col">
                <button id="btn_Listar" class="btn btn-primary btn-block">Listar</button>
            </div>
            <div class="col">
                <button id="btn_Cadastrar" class="btn btn-primary btn-block">Cadastrar</button>
            </div>
           @*  <div class="col">
               <button id="btn_Editar" class="btn btn-primary btn-block">Editar</button>
            </div> *@
        </div>
    </div>
</div>

<!-- Div de Componentes -->
<div id="components" style="margin-top:30px">
    <!-- Listar Rede -->
    <div id="Listar_Rede" class="component" style="display: flex;">
        <p>Conteúdo de Listar Rede</p>
    </div>

    <!-- Cadastrar Rede -->
    <div id="Cadastrar_Rede" class="component" style="display: none;">
       
        <form asp-controller="Rede" asp-action="Criar" method="post">
            <div class="form-group">
                <label for="nome">Nome</label>
                <input type="text" asp-for="Nome" class="form-control" placeholder="Digite o nome da rede">
            </div>
            <div class="form-group">
                <label for="cnpj">CNPJ</label>
                <input type="text" asp-for="Cnpj" class="form-control" placeholder="Digite o CNPJ da rede">
            </div>
            <div class="form-group">
                <label for="endereco">Endereço</label>
                <input type="text" asp-for="EnderecoId" class="form-control" placeholder="Digite o endereco da academia">
            </div>
            <button type="submit" class="btn btn-primary btn-block">Adicionar</button>
        </form>
    </div>

    <!-- Editar -->
    <div id="Editar_Rede" class="component" style="display: none;">
        <p>Conteúdo de Editar Rede</p>
    </div>
</div>

<script>
    // Função para exibir o componente correspondente
    function exibeComponente(index) {
        var componentes = document.querySelectorAll('.component');
        componentes.forEach((comp, idx) => {
            if (idx === index) {
                comp.style.display = "flex";
            } else {
                comp.style.display = "none";
            }
        });
    }

    // Adiciona event listeners aos botões
    document.getElementById("btn_Listar").addEventListener("click", function() {
        exibeComponente(0); // Exibe Listar_Rede
    });

    document.getElementById("btn_Cadastrar").addEventListener("click", function() {
        exibeComponente(1); // Exibe Cadastrar_Rede
    });

    document.getElementById("btn_Editar").addEventListener("click", function() {
        exibeComponente(2); // Exibe Editar_Rede
    });

</script>